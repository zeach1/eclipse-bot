/*const messenger = require('../helper/messenger.js');

module.exports = {
  name: 'donation',
  type: 'developer',
  description: 'Donation set up for war',

 execute: async function(message, param) {
    const { args } = param;

    switch (args[0]) {
      case 'valk/witch':  return this.valkwitch()   
      case 'hound/golem': return this.houndgolem()
      
      case 'loon/wiz':
      case 'topoff1':
      case 'topoff2':
      default:          return messenger.sendArgumentError(message, this, 'This argument does not exist');
                   }
 },

 valkwitch: async function(message, num) {
 houndgolem: async function(message, num){
 loonwiz: async function(message, num){
 topoff1: async function(message, num){
 topoff2: async function(message, num){
    
  },
  */