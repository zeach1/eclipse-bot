// const gameManager = require('../helper/gameManager.js');

module.exports = {
  name:'play',
  type: 'developer',
  description: 'Play a game with other members!',
  usage: '[join <user> | start [-f] | leave]',
/*
  execute: function(message, param) {
    const { args } = param;

  },
  */
};
